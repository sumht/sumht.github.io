# WebSocket

## 核心依赖

> WebSocket 框架，支持多节点的广播

```xml

<dependency>
    <groupId>com.sumht</groupId>
    <artifactId>sumht-starter-websocket</artifactId>
    <version>${lasUpdate.version}</version>
</dependency>
```
# 定时任务

## 核心依赖

> 任务拓展
> 1. 定时任务，基于 Quartz 拓展
> 2. 异步任务，基于 Spring Async 拓展

```xml

<dependency>
    <groupId>com.sumht</groupId>
    <artifactId>sumht-starter-job</artifactId>
    <version>${lasUpdate.version}</version>
</dependency>
```
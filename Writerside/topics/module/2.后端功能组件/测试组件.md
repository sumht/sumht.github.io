# 测试组件

## 核心依赖

> 测试组件，用于单元测试、集成测试

```xml

<dependency>
    <groupId>com.sumht</groupId>
    <artifactId>sumht-starter-test</artifactId>
    <version>${lasUpdate.version}</version>
</dependency>
```